group 'com.soomla.android-core'
version '1.3.0'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile 'com.squareup:otto:1.3.2'
    compile files('libs/keeva.jar')
}
